<Page x:Class="PopupBlocker.Views.Pages.SettingPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      xmlns:vm="clr-namespace:PopupBlocker.ViewModels"
      xmlns:controls="clr-namespace:PopupBlocker.Controls"
      mc:Ignorable="d" 
      d:DataContext="{d:DesignInstance vm:SettingViewModel}"
      d:DesignHeight="450" d:DesignWidth="800"
      ui:Design.Background="{DynamicResource ApplicationBackgroundBrush}"
      ui:Design.Foreground="{DynamicResource TextFillColorPrimaryBrush}"
      Foreground="{DynamicResource TextFillColorPrimaryBrush}"
      Title="SettingPage">

    <StackPanel>
        <controls:CardToggleSwitch Content="开机自启"/>
        <controls:CardToggleSwitch Content="启动拦截" IsChecked="{Binding IsEnableInterceptor}"/>
        <controls:CardToggleSwitch Content="启动日志" IsChecked="{Binding IsEnableLog}"/>
    </StackPanel>
</Page>
